spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/InTempo
    username: admin
    password: admin123

  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    keys:
      public: classpath:app.pub
      private: classpath:app.key